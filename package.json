{
  "name": "demo-migration-swarm",
  "version": "0.0.0",
  "description": "Vanilla ES-module HTML/CSS/JS expense-splitter - the fixed migration target for the Migration Swarm hackathon project.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.11.0"
  },
  "license": "MIT"
}
